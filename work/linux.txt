检查bash补全功能是否安装
# CentOS/RHEL系统
yum install bash-completion -y

# Ubuntu/Debian系统
apt-get install bash-completion -y

# 安装后重新加载bashrc
source ~/.bashrc

查看服务器公网IP: curl ipconfig.im
===============================


查看所有远程仓库
git remote -v show origin

git fetch -p: 在删除远程分支后，使用此命令可以清理掉本地跟踪的已不存在的远程分支

# 列出所有本地分支
$ git branch

# 列出所有远程分支
$ git branch -r

# 列出所有本地分支和远程分支
$ git branch -a

# 新建一个分支，但依然停留在当前分支
$ git branch [branch-name]

# 新建一个分支，并切换到该分支
$ git checkout -b [branch]

# 新建一个分支，指向指定commit
$ git branch [branch] [commit]

# 新建一个分支，与指定的远程分支建立追踪关系
$ git branch --track [branch] [remote-branch]

# 切换到指定分支，并更新工作区
$ git checkout [branch-name]

# 切换到上一个分支
$ git checkout -

# 建立追踪关系，在现有分支与指定的远程分支之间
$ git branch --set-upstream [branch] [remote-branch]

# 合并指定分支到当前分支
$ git merge [branch]

# 选择一个commit，合并进当前分支
$ git cherry-pick [commit]

# 删除分支
$ git branch -d [branch-name]

# 删除远程分支
$ git push origin --delete [branch-name]
$ git branch -dr [remote/branch]


忽略已经被跟踪的文件或目录：如果某个文件或目录已经被Git跟踪，该命令会将文件或目录从Git仓库中删除并保留在本地磁盘
git rm –cached <文件或目录路径>


给可写权限
chmod -R 777 /www/wwwroot/webman/runtime

放弃本地git修改： git reset --hard HEAD      
永久删除未跟踪的文件  git clean -fd

vim 清除打开的文件  gg dG


